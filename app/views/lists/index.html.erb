<h1>All my Lists</h1>

<% @lists.each do |list| %>
  <li><%= link_to list.name, list_path(list) %></li>
<% end %>

<button class="mt-2 btn btn-danger"><%= link_to "Add a List", new_list_path %></button>
